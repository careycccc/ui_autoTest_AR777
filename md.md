下拉逻辑：要求每一个父用例当只有父用例的时候就显示一个按钮，按钮的尺寸有父用例的名字自适应，当父用例下面有子用例，就是子用例注册了这个父用例，此时测试报告的显示就是父用例是一个下拉框，点击下拉框显示子用例，子用例的尺寸也是自适应的，子用例的点击事件是跳转到子用例的测试报告页面，父用例的点击事件是跳转到父用例的测试报告页面，下拉框第一项就是父用例，并且一个页面只有一个测试用例，每次使用switchToPage切换的时候一定要保存当前路由，因为在报告中页面和路由是一一对应的关系，测试报告页面的排序是：首页，登录页，登录成功页，后面就是依次注册的父用例